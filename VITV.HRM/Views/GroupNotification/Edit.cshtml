@model Notification
<div class="modal-header no-padding">
    <div class="table-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">
            <span class="white">&times;</span>
        </button>
        Sửa thông báo
    </div>
</div>
@using (Ajax.BeginForm("Edit", "GroupNotification", null, new AjaxOptions
            {
                OnSuccess = "OnSuccessEditNotifi",
                HttpMethod = "Post",
            }, new { @class = "form-horizontal" }))
{
    @Html.Hidden("GroupId", Model.Id)
    <div class="modal-body">
        <div class="form-group">
            <label class="col-sm-2 control-label no-padding-right" for="form-title"> Tiêu đề </label>

            <div class="col-sm-10">
                @*@Html.EditorFor(model => model.Title)*@
                <input type="text" id="form-title" placeholder="Title" name="Title" value="@Model.Title" class="col-xs-12 col-sm-12">
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-2 control-label no-padding-right" for="form-content"> Nội dung </label>

            <div class="col-sm-10">
                @*@Html.TextAreaFor(model => model.Content)*@
                <textarea class="form-control" id="form-content" name="Content" placeholder="Content">@Model.Content</textarea>
            </div>
        </div>
        <div class="form-group">
        </div>


    </div>

    <div class="modal-footer no-margin-top">
        <button type="submit" class="btn btn-sm btn-danger pull-right">
            <i class="ace-icon fa fa-times"></i>
            OK
        </button>

    </div>
}
